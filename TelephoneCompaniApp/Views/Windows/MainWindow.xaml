<Window x:Class="TelephoneCompaniApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:l="clr-namespace:TelephoneCompaniApp"
        DataContext="{Binding MainWindowModel, Source={StaticResource Locator}}"
        Title="{Binding Title}"
        Width="800" Height="450">
    <DockPanel>
        <Menu DockPanel.Dock="Top" IsMainMenu="True">
            <Menu.ItemsPanel>
                <ItemsPanelTemplate>
                    <DockPanel LastChildFill="False"/>
                </ItemsPanelTemplate>
            </Menu.ItemsPanel>
            <MenuItem Header="Файл">
                <MenuItem Header="Поиск" Command="{Binding ShowStreetWindowCommand}"/>
                <MenuItem Header="Улицы" Command="{StaticResource CloseWindowCommand}"/>
                <MenuItem Header="Выгрузить CSV" Command="{StaticResource CloseWindowCommand}"/>
                <MenuItem Header="Выход" Command="{StaticResource CloseWindowCommand}"/>
            </MenuItem>
        </Menu>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem DockPanel.Dock="Right">
                <ResizeGrip/>
            </StatusBarItem>
            <StatusBarItem Content="{Binding Status}"/>
        </StatusBar>
        <Grid>
            <DataGrid ItemsSource="{Binding _MainDataGridItems}" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" />
                    <DataGridTextColumn Header="Street" Binding="{Binding Street}" />
                    <DataGridTextColumn Header="House Number" Binding="{Binding HouseNumber}" />
                    <DataGridTextColumn Header="Home Phone Number" Binding="{Binding HomePhoneNumber}" />
                    <DataGridTextColumn Header="Work Phone Number" Binding="{Binding WorkPhoneNumber}" />
                    <DataGridTextColumn Header="Mobile Phone Number" Binding="{Binding MobilePhoneNumber}" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </DockPanel>
</Window>
